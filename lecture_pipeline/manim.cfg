[CLI]
# Quality is set per-render in render.py; do NOT lock it here
preview = False
save_last_frame = False
write_to_movie = True

# Output settings
output_file = 
verbosity = WARNING
notify_outdated_version = False

# LaTeX settings - Use dvisvgm 3.x for better rendering
tex_template = 
text_to_replace = 

# Background
background_color = WHITE

# dvisvgm optimization flags
# -qh: high quality mode
# -pk: use pk fonts for better rendering
# These flags improve fraction line rendering
