server:
  port: 8899

spring:
  application:
    name: seeview

manim:
  # 本地子进程渲染（see 工程自包含 lecture_pipeline 引擎）
  python: ${user.dir}/lecture_pipeline/.venv/Scripts/python.exe
  engine-dir: ${user.dir}/lecture_pipeline
  timeout: 600s
  connect-timeout: 10s
  # 阿里云 CosyVoice TTS key（通过环境变量传给 python 子进程）；也可用系统环境变量 DASHSCOPE_API_KEY
  dashscope-key: ${DASHSCOPE_API_KEY:}

  # 远程微服务地址（/get 接口走 HTTP 客户端模式时用，本方案默认不走）
  base-url: http://localhost:8000
